export type PricingType = {
  img: string;
  price: string;
  noOfUsers: number;
  noOfServices: number;
  buttonText: string;
  link: string;
};

export type TestimonialType = {
  img: string;
  testimonial: string;
  role: string;
  horizontal?: boolean;
};
